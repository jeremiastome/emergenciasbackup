({
  obtenerDetalles : function(component, event, helper) {
    helper.obtenerDetalles(component);
  },

  toggleModal : function(component, event, helper) {
    helper.showHideModal(component);
  }
})