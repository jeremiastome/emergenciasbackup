({
  doInit: function (component, event, helper) {
    helper.doInit(component, helper);
  },

  obtenerServicios: function (component, event, helper) {
    helper.obtenerServicios(component, helper);
  }
})